{
  "name": "night-city-net",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Night City Net cyberpunk messenger foundation for Capacitor and Firebase",
  "scripts": {
    "build:web": "node scripts/build-web.mjs",
    "test": "node --test tests/*.test.mjs",
    "check": "npm run test && npm run build:web && node scripts/verify-web.mjs",
    "cap:add:android": "npx cap add android",
    "cap:sync": "npm run build:web && npx cap sync android",
    "android:debug": "npm run cap:sync && cd android && ./gradlew assembleDebug",
    "android:release": "npm run cap:sync && cd android && ./gradlew assembleRelease"
  },
  "dependencies": {
    "@capacitor/android": "8.4.1",
    "@capacitor/app": "8.0.1",
    "@capacitor/core": "8.4.1",
    "@capacitor/haptics": "8.0.1",
    "@capacitor/keyboard": "8.0.1",
    "@capacitor/push-notifications": "8.0.1",
    "@capacitor/status-bar": "8.0.1",
    "firebase": "12.16.0"
  },
  "devDependencies": {
    "@capacitor/cli": "8.4.1"
  },
  "engines": {
    "node": ">=22"
  }
}
